entré : linked_list containing strings
si input NULL, return.
searche for pipe. if pipe found, call pipe management function
read each str of args : 
	if output redirection found, call apropriate output redirect fct, then jump two args (redirect + file)
	if input redirection found, call apropriate input redirect fct, then jump two args (same)
	else, add string to linked list std_args
once done, turn std_args into an array
execute cmd;

